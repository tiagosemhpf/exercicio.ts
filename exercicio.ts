// Função de multiplicação
function funcaoDeMultiplicacao(a: number, b: number): number {
    return a * b;
}

// Função de saudação
function funcaoDeSaudacao(seuNome: string): string {
    return "Olá " + seuNome;
}